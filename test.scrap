//Adds two numbers

SYMBOL		TEMP	0
MAIN					# Entry Point
    LOADi   7
    LOADm	63
    STOREA
    LOADi   5
    ADD
    STORE
    JMP     HALT

HALT					# Infinite Loop to "halt" the CPU
    NOP
    JMP HALT
